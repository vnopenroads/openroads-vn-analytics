all:
  children:
    openroads-analytics:
      hosts:
        cba-worldbank
      vars:
        ansible_python_intepreter: /usr/bin/python3
    localhost:
      hosts:
        127.0.0.1
      vars:
        ansible_connection: local

  vars:
    ansible_ssh_user: ubuntu
